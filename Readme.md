# Puma + Ruby 2.6 + Systemd

Repository to discuss about https://github.com/puma/puma/issues/1499

## Requirements

1. Docker
2. macOS 10.14.4

## How-to

Run `docker-compose up -d`
Run `docker exec -it web /test-puma.sh`

Check `localhost` on port `3000`
